[project]
name = "test-lab"
version = "0.1.0"
description = "Sandbox for exploring various tools and types of testing"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.135.2",
    "uvicorn>=0.42.0",
]
